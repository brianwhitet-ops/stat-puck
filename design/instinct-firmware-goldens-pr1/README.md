Instinct-locked native 296×128 firmware goldens for PR #1 (REDO visual layer).

Verify:

```bash
python3 firmware/tests/verify_sha256.py design/instinct-firmware-goldens-pr1/SHA256SUMS.txt
```

See `REDO-NOTE.md` and `docs/VISUAL_SPEC.md`.
